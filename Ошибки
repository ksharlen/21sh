В этом тесте нажимается энтр при пустой строке шелл падает:
	$>echo << EOF
	heredoc>
	Ошибка сегментирования


	$>sort << EOF
	heredoc> hello
	heredoc> my
	heredoc> test
	heredoc> EOF
	sort: не удалось прочитать: 'hello'$'\n''my'$'\n''test'$'\n': Нет такого файла или каталога
Не даёт возможность пройти тест из чеклиста:
	$>sort << EOF | cat -e > sorted_poem


В этом тесте после второго heredoc появляется пробел перед первым словом:
	$>echo << EOF << EOF
	heredoc> test1
	heredoc> EOF
	heredoc> test2
	heredoc> EOF
	test1
	 test2


Завершает работу heredoc при вводе EO:
	$>echo << EOF
	heredoc> test
	heredoc> EO
	test
Или:
	$>echo << EOFEOF
	heredoc> test
	heredoc> EO
	test
Или:
	$>echo << DELIMITER
	heredoc> test
	heredoc> DE
	test
